# 创建vite project
npm create vite@latest # 直接选择使用vue框架后，就不会使用ssr了
# 安装依赖
npm install
# 运行构建, 生成输出路径，默认dist
npm run build
# 运行预览，不输出
npm run preview

# 安装three.js依赖, 自动更新package.json
npm install --save three
